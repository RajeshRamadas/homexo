Django>=5.2
djangorestframework>=3.15
django-cors-headers>=4.3
Pillow>=10.0
python-decouple>=3.8
python-dotenv>=1.0          # Auto-loads .env into os.environ
social-auth-app-django>=5.4
twilio>=8.0          # Phone OTP (SMS)

# Development
django-extensions>=3.2
ipython>=8.0

# Production
gunicorn>=21.0
psycopg2-binary>=2.9       # PostgreSQL adapter
# django-storages>=1.14    # S3 / cloud media (optional)
# boto3>=1.34               # AWS SDK (optional)

# Vector DB / RAG Chatbot (Urvashi — Claude + local embeddings)
pgvector>=0.4.0            # pgvector Django ORM integration
anthropic>=0.20.0          # Claude API (Haiku/Sonnet)
sentence-transformers>=2.6 # Local embeddings — all-MiniLM-L6-v2 (384-dim)
